<footer>
  <small>
    <%if current_user%>
      Sign in as <%=current_user.full_name%> (<%= link_to "Log Out", session_path("current"), method: :delete%>)
      <%if current_user.is_admin?%>
        <p><%= link_to "All Users", admin_users_path%> 
      <%end%>
    <%else%>
      <%=link_to "Log In", new_session_path%> | <%= link_to "Sign Up", new_user_path%> 
    <%end%>
  </small>
</footer>
